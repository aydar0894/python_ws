# Benchmark coin API

## Start

To start a project run:

  `export FLASK_ENV=production`
  
  `export FLASK_APP=main.py`
  
  `flask run`

## Dependecies:
 - Flask >= 1.0.6
 - python 3
 - pandas
 - numpy
